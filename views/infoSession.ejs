
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>세션 객체의 메소드를 사용하기</title>
    </head>
    <body>
        <center>
            <h3>
                예제 
            </h3>
        </center>
        <hr/>
        <% var id_str = session.id;
        var lasttime = session.LastAccessedTime;
        var createdtime = session.CreationTime; 
        var time_used = (lasttime - createdtime) / 60000; 
        int inactive = session.MaxInactiveInterval / 60;
        var b_new = session.isNew();%>[1] 
        `<%= session.getId() %>`
        <br/>
        <hr/>당시이 웹 사이트에 머문 시간은 `<%= time_used %>` 분입니다.<br/>
        <hr/> 세션의 유효 시간은 `<%= inactive %>` 분입니다.<br/>
        <hr/>
        세션이 새로 만드어 졌나요???<br/>
        `<%  if (b_new) { %>`
            예!! 새로운 세션을 만들었습니다.
        `<% } else { %>`
            아니오!! 새로운 세션을 만들지 않았습니다
        `<% } %>` <hr/>
    </body>
</html>

